import React, { Compoment } from 'react';
import Register from './../../components/login/index';

export default class Login extends Compoment {
    render() {
        return (
            <div>
                <Register />
            </div>
        )
    }
}