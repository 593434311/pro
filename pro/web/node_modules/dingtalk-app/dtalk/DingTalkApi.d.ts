import { UserInfoVo } from './UserInfoVo';
export declare class DingTalkApi {
    /**
     * 免登
     * @param authCode 临时授权码
     */
    login(authCode: string, ...userField: string[]): Promise<{
        token: string;
        user: any;
    }>;
    /**
     * 生程客户端js api 签名
     * @param querystring url参数
     */
    signature(querystring?: string, domain?: string): Promise<{
        agentId: number;
        corpId: string;
        timeStamp: number;
        nonceStr: string;
        signature: string;
        type: number;
    }>;
    /**
    * 解析token
    * @param authorization
    */
    decode(authorization: string): UserInfoVo;
}
declare let dingTalkApi: DingTalkApi;
export default dingTalkApi;
